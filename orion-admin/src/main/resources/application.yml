spring:
  profiles:
    active: dev
  application:
    name: orion-admin
  data:
    redis:
      host: r-bp1yfwh6hywku5hhhcpd.redis.rds.aliyuncs.com
      database: 0
      port: 6379
      password: Goodwell99
      timeout: 250ms
      lettuce:
        pool:
          min-idle: 0
          max-idle: 8
          max-active: 8
          max-wait: -1ms
  datasource:
    url: jdbc:mysql://rm-cn-zxu37mhva000feho.rwlb.rds.aliyuncs.com:3306/orion?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: Goodwell99

sa-token:
  token-name: orion-token
  token-prefix: Bearer
  timeout: 3600
  active-timeout: 7200
  is-concurrent: false
  is-share: true
  isReadCookie: false
  token-style: uuid
  is-log: true


knife4j:
  enable: true
  basic:
    enable: true
    username: root
    password: root
  setting:
    swagger-model-name: 数据模型
    enable-version: true
    enable-footer: false

mybatis-plus:
  # 设置Mapper接口所对应的XML文件位置，如果你在Mapper接口中有自定义方法，需要进行该配置
  mapper-locations: classpath*:mapper/*.xml
  # 设置别名包扫描路径，通过该属性可以给包中的类注册别名
  type-aliases-package: io.ispacc.orion.admin
